
import edu.berkeley.bid.comm._;
import BIDMat.AllReducer;

val sim = new AllReducer
sim.makeSim(16,100000,1000,100,2);

val net = sim.network;
val m0 = net.machines(0);

val l0 = m0.layers(0);
